public class Solution {
    public int climbStairs(int A) {
        return fib(A+1);
    }
    
    public int fib(int n) {
        if(n < 2) {
            return n;
        }
        return fib(n-1)+fib(n-2);
    }
}
